import { Card } from '../ui/Card';
export function MonthlyChart({ monthly }){const max=Math.max(...monthly.map(m=>Math.max(m.revenue,m.expenses||0)),1);return <Card><div className="p-6"><h3 className="text-lg font-bold">Mesečni trend</h3><div className="mt-6 grid grid-cols-12 items-end gap-2 overflow-x-auto pb-2">{monthly.map(item=><div key={item.month} className="flex min-w-16 flex-col items-center gap-2"><div className="flex h-44 w-full items-end gap-1 rounded-2xl bg-slate-50 p-2"><div className="w-1/2 rounded-t-xl bg-sky-500" style={{height:`${Math.max(4,(item.revenue/max)*100)}%`}}/><div className="w-1/2 rounded-t-xl bg-slate-300" style={{height:`${Math.max(4,((item.expenses||0)/max)*100)}%`}}/></div><span className="text-xs font-medium text-slate-500">{item.month.slice(0,3)}</span></div>)}</div></div></Card>;}
